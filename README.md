# Pomodoro

This repository is currently using a feature-branch workflow.

- `main` is the baseline branch so you can open a PR that shows the full initial app diff.
- Open the PR from `feature/pomodoro` into `main` to review and merge the complete app.

