# CLI Commands

You can control the running instance from a terminal.

Examples:

```powershell
pom start
pom pause
pom resume
pom stop
pom open
pom exit
```

`start` supports options:

```powershell
pom start --work 25 --break 5 --long 15 --cycles 4 --auto --popup --sound
pom start --force
```

